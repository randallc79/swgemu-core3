# SWGEmu Core3: Docker Build

This fork is only for building a working Docker Container.

Currently, still testing.  Once a working container is verified.  I will publish all the info on how to setup and use.

## Notes

git fetch upstream
git merge upstream/unstable
git push
